import React from 'react';
import { navigationBarStyle } from './styles';

const NavigationBar: React.FC = () => {
    return <nav css={navigationBarStyle}>Future navigation area</nav>;
};

export { NavigationBar };
